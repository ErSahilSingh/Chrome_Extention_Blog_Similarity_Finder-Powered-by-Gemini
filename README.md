# React + Vite

The extension should detect the currently active tab URL (if it’s a blog/article page) and use Gemini API to generate a list of similar blog/article links related to the same topic.
